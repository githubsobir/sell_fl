import 'package:flutter/material.dart';

class RegisAuth extends StatefulWidget {
  @override
  _RegisAuthState createState() => _RegisAuthState();
}

class _RegisAuthState extends State<RegisAuth> {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
    // body:
  }

  Widget reg() {
    return Container(
      child: Text("Reg"),
    );
  }

  Widget log() {
    return Container(
      child: Text("Reg"),
    );
  }
}
